[phases.setup]
nixPkgs = ["python310", "playwright", "python310Packages.pip"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt",
  "playwright install"
]

[phases.start]
cmd = "python3 aviso_bot.py"
